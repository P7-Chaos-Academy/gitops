apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: my-backend-app
  namespace: argocd
spec:
  project: default
  source:
    repoURL: git@github.com:P7-Chaos-Academy/devops.git
    targetRevision: main
    path: strato-api # The folder in your manifest repo
  destination:
    server: https://kubernetes.default.svc
    namespace: strato-api # The namespace to deploy to
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true