apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nodekiller
  namespace: argocd
spec:
  project: default
  source:
    repoURL: git@github.com:P7-Chaos-Academy/gitops.git
    targetRevision: main
    path: pi/node-killer
  destination:
    server: https://kubernetes.default.svc
    namespace: nodekiller # The namespace to deploy to
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
